import {DefaultTheme} from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#ebaccb',
    accent: '#7162dd',
    text: '#e3706a',
    gray: '#e3e3e3',
  },
};

export default theme;
